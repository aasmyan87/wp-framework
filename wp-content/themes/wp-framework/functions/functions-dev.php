<?php
/**
 * Change all file versions
 */
if ( ! defined( '_S_VERSION' ) ) {
    // Replace the version number of the theme on each release.
    define( '_S_VERSION', '6.0.0' );
}

/**
 * Enable(true) id's of inline style tags
 */
if ( ! defined( 'INLINE_CSS_IDS' ) ) {
    // Replace the version number of the theme on each release.
    define( '_INLINE_CSS_IDS', true );
}
